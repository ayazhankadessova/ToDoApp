import React from 'react'
import { FormContainer, Button, Input } from './styles'

function Form() {
  return (
    <FormContainer>
      <Input />
      <Button>Add</Button>
    </FormContainer>
  )
}

export default FormContainer
